import data from './data'

function map(array, fn) {
  //build a function that 
  //uses data to get and array
  //of trainers
}

function filter(array, fn) {
  //filter out the trainers you
  //can't beat
}

export default data